import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Child_helpline = ({navigation}) => {
    return(
        <View>
            <Text h1>Child_helpline</Text>
        </View>
    )
}

export default Child_helpline